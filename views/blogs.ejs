<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sample Page</title>
    <link rel="stylesheet" href="blogs.css">
</head>
<body>
    
    <%- include('components/navbar') %>

    <div class="container">

           <% if( blogs.length == 0) { %>
                <h1> No Blogs</h1>
           <% } else {  %>
                <% blogs.forEach((blogs) => { %>
                    <div class="card">
                        <h2> <%- blogs.title %> </h2>
                        <h3> <%- blogs.subtitle %> </h3>
                        <p> <%- blogs.description.length > 30 ? blogs.description.slice(0, 30) :blogs.description %>... </p>
                        <a href="/single/<%-blogs.id %>">Read More</a>
                    </div>
                
                <% }) %>
           <% }  %>


        
        

    </div>

    <%- include('components/footer') %>
</body>
</html>
